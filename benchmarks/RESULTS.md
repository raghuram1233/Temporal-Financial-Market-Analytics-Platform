# Benchmark Results

Generated by `python -m benchmarks.run_benchmarks`. Every figure below was
measured on the machine and dataset described here - re-run the suite to
reproduce them on your own hardware.

## Headline

| Measurement | Result |
|---|---|
| Ingest throughput | **46,499 rows/sec** (COPY into hypertable) |
| Storage compression | **13.2x** (708.8 MB -> 53.5 MB) |
| Latest-price lookup | **1,443.8x** faster via cache table |
| OHLC 30d | **16.9x** faster via continuous aggregate |
| OHLC 365d | **32.5x** faster via continuous aggregate |

## Environment

| | |
|---|---|
| Database | PostgreSQL 16.3 |
| TimescaleDB | 2.15.2 |
| Host | Windows 10 (AMD64) |
| Max parallel workers | 16 |
| Dataset | 5,000,000 rows, 25 assets, 730 days |
| Measured | 2026-09-04 15:51 UTC |

## 1. Ingest throughput

Bulk load via `COPY` into the `market_data` hypertable. The per-row
price-cache trigger is dropped for the duration of the load and the
cache is repopulated with a single set-based statement afterwards -
otherwise the trigger fires once per row and dominates the measurement.

| Metric | Value |
|---|---|
| Rows loaded | 5,000,000 |
| Wall time | 107.5 s |
| **Throughput** | **46,499 rows/sec** |

## 2. Compression

TimescaleDB native columnar compression on chunks older than 7 days
(`compress_segmentby = 'asset_id'`).

| Metric | Value |
|---|---|
| Chunks total | 106 |
| Chunks compressed | 104 |
| Size before | 708.8 MB |
| Size after | 53.5 MB |
| **Compression ratio** | **13.2x** |
| Space saved | 655.2 MB (92.4%) |
| Time to compress | 5.4 s |

## 3. Query latency

Each query is warmed up, then timed over repeated runs. Times in
milliseconds; lower is better.

### Latest price for all assets

`latest_prices_cache` is maintained by a trigger so the hot path never
scans the hypertable.

| Method | p50 | p95 | p99 | Speedup |
|---|---:|---:|---:|---:|
| `latest_prices_cache` lookup | 0.38 | 0.61 | 0.61 | **1,443.8x** |
| `DISTINCT ON` over `market_data` | 550.53 | 570.72 | 570.72 | baseline |

### OHLC candles (single asset)

Continuous aggregate vs. bucketing raw ticks at query time.

| Window | Method | p50 | p95 | Speedup |
|---|---|---:|---:|---:|
| 30d | `market_data_daily` (continuous aggregate) | 1.04 | 1.24 | **16.9x** |
| 30d | raw `time_bucket` aggregation | 17.56 | 18.56 | baseline |
| 365d | `market_data_daily` (continuous aggregate) | 1.92 | 2.39 | **32.5x** |
| 365d | raw `time_bucket` aggregation | 62.56 | 64.12 | baseline |

### Effect of compression on scan queries

The same range scans, measured before and after compressing old chunks.

| Query | Uncompressed p50 | Compressed p50 | Change |
|---|---:|---:|---:|
| 30-day aggregate scan | 8.65 | 16.73 | 1.93x slower |
| 365-day aggregate scan | 31.98 | 40.82 | 1.28x slower |
| SMA-7 + volatility window | 47.31 | 73.46 | 1.55x slower |

Compression is not a free speed-up, and these numbers show the real
tradeoff: storage falls sharply while full-range aggregate scans get
*slower*, because every batch must be decompressed before it can be
aggregated. TimescaleDB compression pays off for queries that filter on
the `segmentby` column and for storage cost - not for wide scans over
historical data. The continuous aggregate above is the right answer for
that access pattern, which is why the charts read from it rather than
from raw ticks.

## 4. Query plans

Confirmation that the intended access paths are actually used.

### OHLC via continuous aggregate

```
Custom Scan (ChunkAppend) on _materialized_hypertable_3  (cost=0.28..30.28 rows=40 width=40) (actual time=0.019..0.033 rows=30 loops=1)
  Order: _materialized_hypertable_3.bucket
  Chunks excluded during startup: 10
  Buffers: shared hit=11
  ->  Index Scan Backward using _hyper_3_117_chunk__materialized_hypertable_3_asset_id_bucket_i on _hyper_3_117_chunk  (cost=0.28..15.16 rows=30 width=40) (actual time=0.019..0.030 rows=30 loops=1)
        Index Cond: ((asset_id = 1) AND (bucket >= (now() - '30 days'::interval)))
        Buffers: shared hit=11
Planning:
  Buffers: shared hit=104
```

### OHLC via raw time_bucket

```
GroupAggregate  (cost=2.36..5762.68 rows=2372 width=136) (actual time=0.734..4.732 rows=31 loops=1)
  Group Key: (time_bucket('1 day'::interval, market_data."time"))
  Buffers: shared hit=89
  ->  Result  (cost=2.36..3104.14 rows=209362 width=24) (actual time=0.156..3.129 rows=8218 loops=1)
        Buffers: shared hit=89
        ->  Custom Scan (ChunkAppend) on market_data  (cost=2.36..487.12 rows=209362 width=16) (actual time=0.155..2.641 rows=8218 loops=1)
              Order: time_bucket('1 day'::interval, market_data."time")
              Chunks excluded during startup: 100
              Buffers: shared hit=89
              ->  Sort  (cost=0.00..0.00 rows=0 width=0) (actual time=0.155..0.158 rows=93 loops=1)
                    Sort Key: (time_bucket('1 day'::interval, _hyper_1_101_chunk."time"))
                    Sort Method: quicksort  Memory: 30kB
```

## Reproducing

```bash
docker compose up -d db
python -m benchmarks.run_benchmarks --rows 5000000
```

The suite builds a throwaway `temporal_bench` database from `schema.sql`,
loads synthetic ticks, and drops it again unless `--keep` is passed.
Absolute milliseconds vary with hardware, so the cross-method ratios are
the meaningful result.
